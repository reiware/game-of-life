export const CELL_PADDING = 1;
export const CELL_SIZE = 16;

type Configuration = {
    numberOfRows: number;
    numberOfCols: number;
    cellPadding: number;
    cellSize: number;
}

export const Config:Configuration = {
  cellPadding: 1,
  cellSize: 16,
  numberOfCols: 10,
  numberOfRows: 10,
};
